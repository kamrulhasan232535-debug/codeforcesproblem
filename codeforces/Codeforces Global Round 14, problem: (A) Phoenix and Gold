#include <stdio.h>

int main() {
	int t;
	scanf("%d",&t);
	while(t--){
	    int a,b,sum=0;
	    scanf("%d %d",&a,&b);
	    int array[a];
	    for(int i=0; i<a; i++){
	        scanf("%d",&array[i]);
	        
	    }for(int i=0; i<a; i++){
	        sum+=array[i];
	        if(sum==b){
	            sum-=array[i];
	            int temp=array[i];
	            array[i]=array[a-1];
	            array[a-1]=temp;
	            sum+=array[i];
	        }
	    }if(sum==b){
	        printf("NO\n");
	    }else{
	        printf("YES\n");
	        for(int i=0; i<a; i++){
	            printf("%d ",array[i]);
	        }printf("\n");
	    }
	}

}

