#include <stdio.h>

int main() {
    int t, n;
    scanf("%d", &t);  
    while (t--) {
        scanf("%d", &n);  
        if (n % 3) {
            printf("First\n");
        } else {
            printf("Second\n");
        }
    }

    return 0;
}
