#include<stdio.h>
#include<math.h>
int main(){
    int t;
    scanf("%d",&t);
    int s,x,y;
    while(t--){
        scanf("%d\n",&s);
        scanf("%d %d",&x,&y);
        int min = (x < y) ? x : y;
        int c=(s+min-1)/min;
        printf("%d\n",c);


    }

}
