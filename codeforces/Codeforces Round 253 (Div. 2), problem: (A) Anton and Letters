#include <stdio.h>
#include<string.h>
#include<ctype.h>

int main() {
	char s[10001];
	gets(s);
	int fre[26]={0};
	for(int i=0; s[i]!='\0'; i++){
	    fre[s[i]-'a']+=((isalpha(s[i]))?1:0);
	}int ans=0;
	for(int i=0; i<26; i++){
	    ans+=((fre[i]>0)?1:0);
	}printf("%d\n",ans);
	
}

