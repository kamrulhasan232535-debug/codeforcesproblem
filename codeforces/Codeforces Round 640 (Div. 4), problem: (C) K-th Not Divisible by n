#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        long long n, k;
        scanf("%lld %lld", &n, &k);
        long long ans = k + (k - 1) / (n - 1);
        printf("%lld\n", ans);
    }
    return 0;
}
