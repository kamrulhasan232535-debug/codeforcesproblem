#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        int a;
        scanf("%d", &a);
        int diff = 180 - a;
        if (360 % diff == 0)
            printf("YES\n");
        else
            printf("NO\n");
    }
    return 0;
}
