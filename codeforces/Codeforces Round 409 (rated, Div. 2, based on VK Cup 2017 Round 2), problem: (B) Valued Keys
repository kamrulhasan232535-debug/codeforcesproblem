#include <stdio.h>
#include <string.h>

int main() {
    char x[101], y[101], z[101];
    scanf("%s", x);
    scanf("%s", y);
    
    int len = strlen(x);
    
    for (int i = 0; i < len; i++) {
        if (y[i] > x[i]) {
            printf("-1\n");
            return 0;
        }
        z[i] = y[i];
    }
    
    z[len] = '\0';
    printf("%s\n", z);
    
    return 0;
}
