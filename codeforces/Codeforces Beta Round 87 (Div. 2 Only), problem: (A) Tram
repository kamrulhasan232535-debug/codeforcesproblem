#include <stdio.h>
#include<stdlib.h>

int main() {
	int n;
	scanf("%d",&n);
	int a,b,sum=0;
	int ans=0;
	for(int i=0; i<n; i++){
	    scanf("%d %d",&a,&b);
	    sum-=a;
	    sum+=b;
	    ans=fmax(sum,ans);
	}printf("%d\n",ans);
}

